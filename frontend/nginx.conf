server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://gaia-backend:8000/api/;
        proxy_set_header Host $host;
        client_max_body_size 100M;
    }
}
